Backend & Frontend Instructions that need to code in terminal/command prompt to make the system run:

Backend:
copy .env.example .env
composer install
php artisan key:generate
php artisan migrate
php artisan db:seed
php artisan serve

Frontend:
npm install
npm start
